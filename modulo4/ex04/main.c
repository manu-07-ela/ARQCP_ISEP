#include "asm.h"
#include <stdio.h>

int main(){
    int num1 = 4;
    int num2 = 5;
    int *smaller;

    ptintf("%d", sum_smaller(num1, num2, smaller));
    printf("%d", *smaller);

    return 0;
}

